```mermaid
graph TD;
    input["1. Käyttäjä kirjoittaa tekstin ja napsauttaa 'Tallenna'"] --> send["2. Selain: lähettää palvelimelle vain uuden muistiinpanon (POST /new_note_spa) lataamatta sivua uudelleen"];
    send --> save["3. Palvelin: tallentaa muistiinpanon palvelinpäässä"];
    save --> render["4. JS: lisää uuden muistiinpanon suoraan näytöllä olevaan luetteloon"];
